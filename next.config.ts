import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "swyftin-prod-agape.s3.amazonaws.com", "example.com"],

  },
};

export default nextConfig;
